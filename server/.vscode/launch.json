{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run server",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\main.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "C/C++: g++.exe build active file"
        }
    ]
}